/**
 * ACTION TYPES
 */

 const GET_STATUS = 'GET_STATUS'

 /**
  * INITIAL STATE
  */
    const initialState = {

    }
/**
 *  ACTION CREATORS
 */

 /**
  * REDUCER
  */

  export default function (state = initialState, action) {
      switch(action.type) {
          default:
            return state
      }
  }